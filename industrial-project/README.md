# Pathshala AI

An offline-first browser prototype for the rural-school AI tutor project.

Open `index.html` in a browser for the interface. For the offline cache to work, serve the folder with a local web server (for example, VS Code Live Server). The next implementation phase is replacing the demo tutor replies with an on-device model and adding a real sync API.
